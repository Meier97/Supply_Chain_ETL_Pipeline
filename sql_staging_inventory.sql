CREATE TABLE inventory (
    `Product Name` VARCHAR(255),
    `Year Month` VARCHAR(20),
    `Warehouse Inventory` INT,
    `Inventory Cost Per Unit` DECIMAL(10, 2),
    `Product ID` INT,
    `Index` INT PRIMARY KEY
);